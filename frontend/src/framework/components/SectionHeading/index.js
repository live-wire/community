import React from 'react';
import Text from '../Text';

const SectionHeading = ({children}) => (
	<Text color="rgb(100, 85, 116)" fontSize="13px" fontWeight="600" textTransform="uppercase">{children}</Text>
);

export default SectionHeading;
